import { FechaTransformPipe } from './fecha-transform.pipe';

describe('FechaTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new FechaTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
